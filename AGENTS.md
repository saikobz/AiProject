<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Project skills (auto)

Skills live in `.agents/skills/`. Cursor discovers them automatically; when a task matches a skill, read its `SKILL.md` and follow it before coding.

- `vercel-react-best-practices` — React/Next.js performance and patterns
- `frontend-design` — distinctive UI, typography, layout, visual direction
- `ponytail` — minimal, YAGNI-first implementation

Restore after clone: `npx skills experimental_install` (uses `skills-lock.json`).
